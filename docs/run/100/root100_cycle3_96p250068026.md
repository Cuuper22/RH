# Root 100, cycle 3: reuse the capped block at a tighter strict support

## Terminal outcome

The capped-outer block from `certificate100_cycle1.md` remains admissible when
the full pair window is enlarged from `1.9999` to the still-strict support

\[
\sigma'=\frac{199999}{100000}=1.99999<2.
\]

Its moments and rational quartic dual are unchanged, while the full pair cost
drops.  This gives the strict unconditional improvement

\[
\boxed{
\frac{N_{0,\mathrm{simple}}(T,2T)}{N(T,2T)}
>0.96250068026-o(1).}
\]

The gain over the cycle-1 capped-profile theorem is greater than
`0.00001050`, or about `0.00105` percentage points.  Only the inherited
theorem for every fixed support below two is used.

## 1. Keep the old block exactly

Let \(W_\sigma\) denote the normalized positive Euler profile for the
saturated kernel at full support \(\sigma\).  Retain the old strict values

\[
\mu=\frac{4999}{10000},\qquad g=\frac{103}{400},
\qquad 4\mu=1.9996<2,
\]

and retain the exact symbol constructed at \(\sigma=1.9999\):

\[
r(t)=
\begin{cases}
h,&|t|<g/2,\\
W_{1.9999}(t),&g/2\le |t|\le1/2,
\end{cases}
\]

where \(h\) is fixed by \(\int r=1\).  In particular,

\[
h=0.3395479\ldots .
\]

Nothing about this local block is reoptimized or recomputed.

## 2. Strict embedding in the larger profile

For \(1<\sigma<2\), write \(b=(2-\sigma)/2\).  The Euler profile has the
closed form

\[
u_\sigma(x)=
\begin{cases}
\cos(\sqrt2x),&|x|\le b,\\
A_\sigma\cos(|x|-1/2)
+B_\sigma\sin(\sqrt3(|x|-1/2)),&b\le|x|\le\sigma/2,
\end{cases}
\]

with \(u,u'\) matched at \(b\), and

\[
W_\sigma(t)=\frac{\sigma u_\sigma(\mu t)}
{\int_{-\sigma/2}^{\sigma/2}u_\sigma(x)\,dx}.
\]

Directed interval evaluation of these elementary trigonometric expressions
on the whole outer phase gives

\[
W_{1.99999}(t)-W_{1.9999}(t)>8.0\times10^{-6}
\quad(g/2\le |t|\le1/2).
\]

On the central phase, \(r=h<0.34\), whereas both caps exceed `1.25`.
Therefore

\[
0\le r(t)<W_{1.99999}(t)
\]

everywhere after the same standard taper.  The old block is thus a strict
power-complementary principal compression of the new full matrix.  Since the
block itself is unchanged, its directed moment bounds remain

\[
M_2>0.242110,qquad
M_3>-0.020523,qquad
M_4<0.105889.
\]

## 3. The lower full-matrix cost

Using the same closed Euler formula and directed rounding at
\(\sigma'=1.99999\) gives

\[
D_{\sigma'}=1.06771820498\ldots<1.06771821.
\]

The rational quartic dual from `certificate100_cycle1.md` is also unchanged.
It has

\[
L=0.57918851869157\ldots
\]

and the same directed moment substitution gives

\[
A_P>0.08217339066.
\]

## 4. Fixed point

The accepted stability calculation is therefore

\[
\varepsilon>
\frac{\mu A_P-\frac L2(D_{\sigma'}-1)}{1-L/2}.
\]

Substituting only the outward-rounded inequalities above yields

\[
\varepsilon>0.03021889026,
\]

and hence

\[
\frac{s}{N}>2-1.06771821+0.03021889026
=0.96250068026.
\]

Every support and allocation inequality is strict.  This completes the cycle
without an endpoint passage and without any arithmetic beyond the already
proved fixed-support range \(\sigma<2\).

## Next attack

The remaining deficit is now `0.03749931974`.  Support tightening below two
has essentially no further constant-sized room.  The active branches therefore
continue with the selector--tail coupling and a genuinely mixed higher-moment
observable.
