# Final 95% result

## Result

Combining the completed arithmetic and certificate branches gives

\[
\boxed{
\liminf_{T\to\infty}
\frac{N_{0,\mathrm{simple}}(T,2T)}{N(T,2T)}
>0.9506.}
\]

A fixed strict numerical version of the certificate is

\[
\boxed{
\frac{N_{0,\mathrm{simple}}(T,2T)}{N(T,2T)}
\ge 0.95063832187565-o(1).}
\]

Thus the requested 95% floor is exceeded by more than

\[
0.00063832,
\]

or about \(0.0638\) percentage points.

## Why the conclusion is unconditional within the accepted framework

The two required pieces were proved independently.

1. `agents/arithmetic95_cycle1.md` establishes the saturated pair-trace
   theorem for every fixed support \(\sigma<2\).
2. `agents/certificate95_cycle2.md` gives a fixed certificate at

   \[
   \sigma=1.9999,
   \qquad \mu=0.4999,
   \qquad p=0.83.
   \]

   Its fourth trace has strict total support

   \[
   4\mu=1.9996<2,
   \]

   so all arithmetic used by the nonflat nested block remains inside the
   same proved support range.

The certificate uses the directed pair-cost bound

\[
D_{1.9999}<1.06772567
\]

and an exact rational quartic dual.  The resulting stability increment is

\[
\varepsilon\ge0.01836399187565\ldots,
\]

whence

\[
2-1.06772567+0.01836399187565
=0.95063832187565.
\]

No endpoint passage is needed: both \(1.9999<2\) and
\(4(0.4999)<2\) are strict.  The local top-hat symbol is also strictly
admissible under the optimized full profile, with pointwise allocation
margin greater than \(0.0100\).

## The reading change that crossed 95%

The earlier flat nested block used the fourth moment but discarded the
shape freedom inside that block.  The terminal construction retains that
freedom and uses the mean-one nonflat symbol

\[
r_p(x)=\frac1p\,1_{[-p/2,p/2]}(x),
\qquad p=0.83.
\]

For this symbol, the centered block moments are profile-sensitive rather
than the flat triangular moments.  At \(\mu=0.4999\), they are

\[
M_2=0.2739582798751\ldots,
\quad M_3=-0.1203853391332\ldots,
\quad M_4=0.2026856466565\ldots .
\]

Those three values force substantially more positive-square spectral energy
than the nonsimple directions can absorb.  This is a zero-side reading
improvement; it does not require arithmetic support beyond two.

## Principal artifacts

- `agents/arithmetic95_cycle1.md`: unconditional pair trace for every fixed
  \(\sigma<2\), initially yielding \(93.22826239\%\).
- `agents/root95_cycle4_unconditional_93p8313.md`: merge with the first flat
  quartic stability block, yielding \(93.83133271\%\).
- `agents/certificate95_cycle2.md`: strict nonflat-block certificate yielding
  \(95.06383219\%\).
- `agents/arithmetic95_cycle2.md` through `arithmetic95_cycle4.md`: preserved
  supercritical-support work and the exact depth-nine/divisor-switch
  obstructions, now unnecessary for the 95% target.
- `agents/hybrid95_cycle2.md`: preserved selector--quartic identity and the
  weighted-translation method-class boundary, also superseded by the terminal
  certificate.

## Final status

The 95% target is reached.  Further supercritical arithmetic work is not
needed for this target and was stopped after its active checkpoints were
saved.
